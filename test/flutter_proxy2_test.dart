import 'package:flutter_test/flutter_test.dart';

void main() {
  test('getPlatformVersion', () async {
    // FlutterProxy2 flutterProxy2Plugin = FlutterProxy2();
    // MockFlutterProxy2Platform fakePlatform = MockFlutterProxy2Platform();
    // FlutterProxy2Platform.instance = fakePlatform;
    //
    // expect(await flutterProxy2Plugin.getPlatformVersion(), '42');
  });
}
